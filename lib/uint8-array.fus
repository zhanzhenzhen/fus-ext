fus 2.0.6
import "./main" all

$base64: require("./base64")

toBase64'export: bytes -> $base64.fromByteArray(bytes)

toString'export: bytes ->
    decodeURIComponent(
        Array.from(bytes)
        .map(m -> "%" + m.toString(16)..pad(2).toUpperCase())
        .join("")
    )
